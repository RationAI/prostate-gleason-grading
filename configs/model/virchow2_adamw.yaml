defaults:
  - decode_head@decode_head: embedding_classifier
  - _self_

_target_: ml.embedding_model.EmbeddingGleasonModel
num_classes: 3
lr: ???
decode_head:
  in_features: 2560
  out_features: ${..num_classes}
