# @package _global_

defaults:
  - /experiment/training_and_evaluation/virchow2/data: ???
  - /experiment/training_and_evaluation/virchow2/model: ???
  - _self_

mode: fit

checkpoint: null

data_info: ???
model_name: ???
hparams: ???

metadata:
  run_name: "TRAIN: ${model_name} ${hparams} ${data.dataset.name} ${data_info} Seed=${seed}"
  description: "TRAIN: ${model_name} ${hparams} ${data.dataset.name} ${data_info} Seed=${seed}"
