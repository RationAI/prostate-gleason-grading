# @package _global_

defaults:
  - /experiment/predict/embeddings/virchow2/adamw/base
  - /data/mmciTL_224@data
  - _self_

datamodule:

  predict:
    uris: ["${data.dataset.mlflow_uris.embeddings.virchow2_tiling_val}"]
    paths: null
    embeddings_col: embedding
