# @package _global_

defaults:
  - /experiment/training/mmci2k/embeddings/virchow2/base
  - /model/virchow2_adamw@model
  - _self_

model_name: "Virchow2 (AdamW)"

model:
  lr: ???

metadata:
  run_name: "${model_name} ${data.dataset.name} validation_fold=${validation_fold} lr=${model.lr} seed=${seed}"
