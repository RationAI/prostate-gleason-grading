# @package _global_

defaults:
  - /experiment/training/base
  - /datamodule/embedding_datamodule@datamodule
  - _self_

datamodule:
  batch_size: 1024
  num_workers: 8

metadata:
  run_name: "${model_name} ${data.dataset.name} fold=${datamodule.fold} inverse=${datamodule.invert_fold_selection} seed=${seed}"
